apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: currency-app
  labels:
    app: currency-frontend
spec:
  selector:
    app: currency-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
